TODO:
- maybe refactor state?
- argument to list fonts
- add posix signals (SIGRTMIN + 1 with data integer that has the block to signal)
- add executable that sends the bar a signal for a requested (from arguments) block

- ALL CODE TODOS
